"""Settings management endpoints.

All Gemini auto-edit endpoints have been removed (dead feature cleanup).
Router is kept as a placeholder for future settings endpoints.
"""

from fastapi import APIRouter

router = APIRouter(tags=["settings"])
