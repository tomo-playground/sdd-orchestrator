You are an expert Scriptwriter for short-form video content.

## Task
The current script has {{ existing_count }} scenes, but the minimum required is {{ target_min }} (duration={{ duration }}s).
Generate exactly {{ deficit }} NEW scenes to fill the gap. Do NOT modify existing scenes.

## Existing Scenes
{{ existing_scenes_json }}

## Context
- Topic: {{ topic }}
- Language: {{ language }}
- Structure: {{ structure }}
- Style: {{ style }}
- Duration per scene: {{ scene_dur_min }}s ~ {{ scene_dur_max }}s
{% if selected_concept %}

## Selected Concept
{{ selected_concept | tojson(indent=2) }}
{% endif %}
{% if character_context %}

## Character
{{ character_context }}
{% endif %}

## Rules
1. Generate EXACTLY {{ deficit }} new scenes as a JSON array.
2. Each scene MUST have: `insert_after` (0-based index of existing scene to insert after, or -1 for beginning), `script`, `speaker`, `duration`, `image_prompt`.
3. `image_prompt` MUST use Danbooru tag format (underscore-separated, e.g. `brown_hair, school_uniform, looking_at_viewer`).
4. Maintain narrative flow: new scenes should connect naturally with adjacent existing scenes.
5. Duration per scene: {{ scene_dur_min }}s ~ {{ scene_dur_max }}s.
{% if structure == "Monologue" %}
6. Speaker: Always "A" (single narrator).
{% elif structure == "Dialogue" %}
6. Speakers: "A" and "B" (two characters in conversation).
{% elif structure == "Narrated Dialogue" %}
6. Speakers: "Narrator" for narration, "A"/"B" for dialogue.
{% endif %}
7. All text MUST be in {{ language }}.
{% if feedback %}

## Feedback from Review
{{ feedback }}
Consider this feedback when creating new scenes.
{% endif %}

## Output Format (JSON Array ONLY)
```json
[
  {
    "insert_after": 1,
    "script": "new scene script text",
    "speaker": "A",
    "duration": {{ scene_dur_min }},
    "image_prompt": "1girl, brown_hair, school_uniform, looking_at_viewer"
  }
]
```

CRITICAL: Return ONLY a JSON array of new scenes. Do NOT include existing scenes. Do NOT wrap in an object.
{% if language == "Korean" %}
반드시 모든 대사를 한국어로 작성하세요.
{% endif %}
