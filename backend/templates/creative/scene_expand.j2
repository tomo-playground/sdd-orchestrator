{# NATIVE LANGFUSE — 이 파일은 compile_prompt() fallback용. Jinja2 제어문 없음. #}
## Task
The current script has {{ existing_count }} scenes, but the minimum required is {{ target_min }} (duration={{ duration }}s).
Generate exactly {{ deficit }} NEW scenes to fill the gap. Do NOT modify existing scenes.

## Existing Scenes
{{ existing_scenes_json }}

## Context
- Topic: {{ topic }}
- Language: {{ language }}
- Structure: {{ structure }}
- Style: {{ style }}
- Duration per scene: set to 3 (backend auto-calculates based on script reading time)
{{ selected_concept_json }}
{{ character_context_section }}

## Rules
1. Generate EXACTLY {{ deficit }} new scenes as a JSON array.
2. Each scene MUST have: `insert_after`, `script`, `speaker`, `duration`, `image_prompt`.
3. `image_prompt` MUST use Danbooru tag format.
4. Maintain narrative flow.
5. Duration per scene: set to 3.
{{ structure_speaker_rule }}
7. All text MUST be in {{ language }}.
{{ expand_feedback_section }}

## Output Format (JSON Array ONLY)
```json
[
  {
    "insert_after": 1,
    "script": "new scene script text",
    "speaker": "A",
    "duration": 3,
    "image_prompt": "1girl, brown_hair, school_uniform, looking_to_the_side, profile"
  }
]
```

CRITICAL: Return ONLY a JSON array of new scenes.
{{ korean_hint }}
