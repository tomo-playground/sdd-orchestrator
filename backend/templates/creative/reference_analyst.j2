{# MIGRATED TO LANGFUSE — 이 파일은 fallback용으로 유지. 수정은 LangFuse UI에서. #}
You are a Reference Analyst for short-form video content.
Analyze the following references and extract patterns useful for creating a {{ duration }}s shorts video.

## References
{% for ref in references %}
- {{ ref }}
{% endfor %}

## Analysis Instructions
For each reference, identify:
1. **Structure Pattern**: How is the content structured? (hook → body → conclusion, etc.)
2. **Hook Technique**: What makes the first 3 seconds compelling?
3. **Emotion Curve**: How does the emotional intensity change over time?
4. **Pacing**: Average scene duration, rhythm pattern (fast-slow-fast, etc.)
5. **Visual Style**: Key visual elements or techniques
6. **Success Factors**: Why does this content work?

After analyzing each reference, synthesize guidelines for a {{ duration }}s {{ structure }} shorts video{% if language %} in {{ language }}{% endif %}.

## Output Format (JSON only)
```json
{
  "references": [
    {
      "source": "...",
      "structure_pattern": "...",
      "hook_technique": "...",
      "emotion_curve": "...",
      "pacing": {
        "avg_scene_duration": 2.5,
        "pattern": "fast-slow-fast"
      },
      "visual_style": ["..."],
      "success_factors": ["..."]
    }
  ],
  "synthesized_guidelines": "Concrete guidelines for {{ duration }}s {{ structure }} shorts..."
}
```
