{% if allowed_camera_tags %}

Allowed Camera Tags (use ONLY these for camera category):
{{ allowed_camera_tags | join(', ') }}
{% endif %}
{% if allowed_expression_tags %}

Allowed Expression Tags (use ONLY these for expression category):
{{ allowed_expression_tags | join(', ') }}
{% endif %}
{% if allowed_action_tags %}

Allowed Action Tags (use ONLY these for action/pose category):
{{ allowed_action_tags | join(', ') }}
{% endif %}
{% if allowed_environment_tags %}

Allowed Environment Tags (use ONLY these for environment/location category):
{{ allowed_environment_tags | join(', ') }}
{% endif %}
{% if allowed_mood_tags %}

Allowed Mood Tags (use ONLY these for mood/atmosphere category):
{{ allowed_mood_tags | join(', ') }}
{% endif %}
