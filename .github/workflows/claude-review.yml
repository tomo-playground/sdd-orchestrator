name: Claude Review — PR 자동 리뷰

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write
  actions: read

concurrency:
  group: claude-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    runs-on: [self-hosted, sdd]
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          track_progress: true
          allowed_bots: "claude[bot],coderabbitai[bot]"

          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            CLAUDE.md를 읽고 프로젝트 규칙 기준으로 이 PR을 리뷰하세요.
            중점:
            - CLAUDE.md 규칙 위반
            - 버그/로직 오류
            - 보안 취약점
            - 테스트 누락
            - Data Mapping (Spread Passthrough) 위반

            리뷰만 하세요. 코드 수정은 하지 마세요.
            `mcp__github_inline_comment__create_inline_comment`로 구체적 이슈를 인라인 코멘트로 남기세요.

            리뷰 완료 후 반드시 PR Review를 submit하세요:
            - 심각한 버그/보안 이슈가 있으면: `gh pr review ${{ github.event.pull_request.number }} --request-changes --body "리뷰 요약"`
            - 문제 없으면: `gh pr review ${{ github.event.pull_request.number }} --approve --body "리뷰 요약"`
            - 경미한 제안만 있으면: `gh pr review ${{ github.event.pull_request.number }} --comment --body "리뷰 요약"`

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr review:*),Bash(gh api:*),Read,Glob,Grep"
