/**
 * Re-export from shared hook for backward compatibility.
 * Manage tab continues to use this import path.
 */
export { useVoicePresets as useVoicePresetsTab } from "../../../hooks/useVoicePresets";
export type { EditingPreset, UiCallbacks } from "../../../hooks/useVoicePresets";
export { EMPTY_PRESET } from "../../../hooks/useVoicePresets";
